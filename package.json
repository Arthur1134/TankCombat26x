{
  "name": "tank-combat-26x",
  "version": "0.3.16",
  "private": true,
  "description": "Tank Combat 26x — HTML5 bounce-shot tank campaign",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy:run": "npm run build && rundot deploy",
    "deploy:public": "npm run build && rundot deploy --public"
  },
  "dependencies": {
    "@series-inc/rundot-game-sdk": "^5.28.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
